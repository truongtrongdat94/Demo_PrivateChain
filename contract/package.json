{
  "name": "dotnet-besu-hash-demo-contract",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "ethers": "^6.15.0",
    "hardhat": "^3.0.0"
  }
}
